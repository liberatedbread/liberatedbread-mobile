<?xml version="1.0" encoding="utf-8"?>
<!-- Cold-start splash (API 21+). Uses the brand background generated from
     tool/branding/brand.json, so the launch screen matches the app bar and the
     launcher icon instead of following the stock light/dark window background. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/ic_launcher_background" />

    <!-- You can insert your own image assets here -->
    <!-- <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/launch_image" />
    </item> -->
</layer-list>
