# Copyright 2026 Pigs Can Fly Labs LLC
# SPDX-License-Identifier: Apache-2.0
#
# Keep the things CI itself depends on from rotting quietly.
#
# WHY ACTIONS ARE THE POINT
#
# .github/workflows/ uses eleven third-party actions. They are pinned by COMMIT
# SHA, with the tag they came from in a trailing comment — dependabot reads that
# comment, so pinning costs nothing in maintainability and buys the thing a tag
# cannot give: a tag is a mutable pointer in somebody else's repository, and
# whoever controls it can change what your CI executes, retroactively, without
# a commit appearing anywhere you can see. `tj-actions/changed-files` is the
# worked example — a compromised tag exfiltrated secrets from thousands of
# repositories in March 2025 — and CI here holds signing material and a Codecov
# token.
#
# The weekly bump is still the point. A major tag is also a moving target that
# stops moving: `actions/upload-artifact@v3` was turned off outright, and the
# runs that depended on it did not degrade — they failed, all at once, on a day
# nobody chose. Nothing else in this repo watches for that, and `flutter
# analyze` has no opinion about YAML.
#
# WHAT IS DELIBERATELY NOT HERE
#
#   * pub. pubspec.lock is committed and the app is pinned against a specific
#     Flutter SDK; `flutter pub outdated` currently lists 62 packages held back
#     by constraints, so a dependabot run would open a wall of pull requests
#     that all fail resolution for the same reason. Dependency bumps here follow
#     a Flutter bump, by hand.
#   * The Flutter, NDK, Android SDK and flutter_rust_bridge pins. Those live in
#     ci.yml's env: block (see the header there) and moving them moves every
#     developer environment with them, which is not a bot's decision.
#   * gradle. android/ has three build files and no lockfile, and its versions
#     are not independently movable: the Android Gradle Plugin tracks the
#     Gradle wrapper, both track the JDK the CI image ships, and
#     `compileSdkVersion`/`ndkVersion` are read from the pinned Flutter
#     (README's toolchain table). A bot bumping one corner of that produces a
#     PR whose only honest reviewer action is to close it. Listed here because
#     an ecosystem missing from this file otherwise reads as an oversight.
#
# Grouped and capped so this is one reviewable pull request per ecosystem per
# week rather than a queue that nobody reads, which is how these usually end up
# being switched off.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 2
    groups:
      actions:
        patterns: ['*']
    commit-message:
      prefix: 'ci'

  # The Rust crate. Small dependency set, a committed Cargo.lock, and the `rust`
  # job runs fmt + clippy + tests with --locked — so a bump either passes those
  # or says exactly what broke, which is the ideal shape for an automated pull
  # request.
  #
  # flutter_rust_bridge is IGNORED explicitly. It is pinned `=2.9.0` in
  # Cargo.toml against ci.yml's FRB_VERSION (the codegen) and pubspec.yaml's
  # Dart package, and cannot move without both moving too. This file used to
  # claim "the `=` is what stops a bot from trying" — it is not: dependabot
  # rewrites the requirement itself, and PR #18 did exactly that, bumping the
  # crate alone to =2.12.0. The result was not even a loud break: every job
  # passed except the two DEVICE integration jobs, where all four suites ran
  # green in-app and the host harness still exited 1 having counted only three
  # of them — on both platforms, on both retry attempts. A version skew that
  # manifests as a dropped test result is precisely the kind of red a bot PR
  # cannot explain, so the bot does not get to propose it. FRB moves by hand,
  # all three pins at once.
  - package-ecosystem: cargo
    directory: /rust
    schedule:
      interval: weekly
    open-pull-requests-limit: 2
    groups:
      cargo:
        patterns: ['*']
    ignore:
      - dependency-name: flutter_rust_bridge
    commit-message:
      prefix: 'rust'
